The repository is aimed for sharing Capstone Projects
The first of these implements numerical methods for gradient descent, for a given function.
The relevant subset has an upper bound where the function has a kink (a point of continuity, but not differentiability)
Subject to appropriate default values (which could be subject to modification), 3 trajectories are characterized by their initial value.
The iterations are shown to reasonably approximate the given function's global minimizer
